# --- Database (Hostinger MySQL) ---
# DB "president_db" / user "app_user" created in hPanel under the u565363010_ prefix.
# Fill in <PASSWORD> and srv1158.hstgr.io yourself (localhost if the app runs on this same
# Hostinger account; otherwise the external host from hPanel > Remote MySQL,
# with your IP allowlisted there).
DATABASE_URL ="mysql://u565363010_app_user:eP59r9V3edZmgY4bYTZCVUD8@srv1158.hstgr.io:3306/u565363010_president_db"

# --- Auth.js (admin panel login) ---
AUTH_SECRET="b85WahTJJwavG9yymbkoH4ERUWGyIs/3BACmTZiXCgg="

# Full URL of the deployed site (used by Auth.js). For local dev leave as-is.
NEXTAUTH_URL="https://presidentfurniturebd.com"

NEXT_PUBLIC_SUPABASE_URL="https://aouozsyqktvjnfvmwumg.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY ="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImFvdW96c3lxa3R2am5mdm13dW1nIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODQ1Njc4NTAsImV4cCI6MjEwMDE0Mzg1MH0.PLpXt_rQywLGoFknhB-z0nY3psAmh_3qAzGkqqb1Bcw"
SUPABASE_STORAGE_BUCKET ="product-images"
# Product images upload straight to disk now (public/uploads/products) —
# no Supabase/storage env vars needed any more.

# --- GitHub issue integration (website "Report an issue" -> opens a GitHub issue) ---
# Unset: reports still save locally in the admin Issues tab, just aren't pushed to GitHub.
# GITHUB_TOKEN=
GITHUB_REPO_OWNER="sayeed-titan"
GITHUB_REPO_NAME="furniture_ecommerce_with_admin_panel"

# --- Email notifications (new leads + issue reports) ---
# Unset: notifications are silently skipped rather than failing.
# RESEND_API_KEY=
EMAIL_FROM="President Furniture <alerts@yourdomain.com>"
NOTIFY_EMAIL="you@yourbusiness.com"

# --- Payments (SSLCommerz) ---
# Unset: checkout only offers Cash on Delivery until real sandbox/live credentials are added.
# Sandbox credentials are self-serve: https://developer.sslcommerz.com/registration/
# SSLCOMMERZ_STORE_ID=
# SSLCOMMERZ_STORE_PASSWORD=
SSLCOMMERZ_SANDBOX="true"

# --- Admin seed/reset login ---
SEED_ADMIN_EMAIL="admin@presidentfurniture.com"
SEED_ADMIN_PASSWORD="Admin123"

